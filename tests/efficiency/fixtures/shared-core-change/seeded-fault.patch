--- a/src/core.py
+++ b/src/core.py
@@ -1,3 +1,3 @@
 def normalize(value: str) -> str:
     """Core helper imported by many consumers."""
-    return value.strip().lower()
+    return "".join(value.split()).lower()
